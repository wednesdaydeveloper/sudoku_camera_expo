# Phase 3: カメラ／画像取得画面

## 目的

撮影またはライブラリ選択で画像を取得し、後続の盤面検出に渡す画面を実装する。Phase 2 のソルバと合わせて「手動入力で解ける」基本UXがこの段階で動き始める。

## 完了条件 (Definition of Done)

- [ ] `src/screens/CameraScreen.tsx` 実装
- [ ] カメラ・写真ライブラリのパーミッション要求／拒否時のフォールバックUI
- [ ] `expo-camera` で撮影ができる
- [ ] `expo-image-picker` でライブラリから選択ができる
- [ ] 撮影/選択後にプレビュー表示 → 「解析する」ボタンで次画面へ遷移
- [ ] `App.tsx` を書き換え、状態ベースの画面遷移を導入
- [ ] iOS Simulator もしくは実機で起動確認（Web/Expo Go では不可な場合は記録）
- [ ] Gitコミット（日本語メッセージ）

## 設計判断

実装時に追記。以下を判断する:
- 撮影UIはネイティブUIを使うか自作か
- 縦/横の制約
- プレビューの再撮影UX

## 実装内容

実装後に追記。

## 動作確認

実装後に追記。実機/シミュレータの種類、確認した操作、撮影/選択の成功例を記録。

## 次Phaseへの引き継ぎ

完了時に追記。

## ステータス

⚪ 未着手
